name: turbolytics
description: An efficient, user-friendly way to implement logs, analytics and crash reports into your projects.
version: 1.0.0
resolution: workspace
homepage: https://github.com/codaveto/turbolytics

environment:
  sdk: ">=3.5.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  get_it: ^8.0.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
